(defcfg
  block-unmapped-keys yes
  process-unmapped-keys yes
  log-layer-changes no
  concurrent-tap-hold yes
)
(defsrc
  grv  1    2    3    4    5    6    7    8    9    0    -    =    bspc
  tab  q    w    e    r    t    y    u    i    o    p    [    ]    \
       a    s    d    f    g    h    j    k    l    ;    '    ret
  lsft z    x    c    v    b    n    m    ,    .    /    rsft
  lctl lmet lalt           spc            ralt rmet rctl
)
(deflayer modified
  _    q    w    e    r    t    y    y    u    i    o    p    _   _
  esc  @aM  @sA  @dC  @fS  g    _    h    @jS  @kC  @lA  @;M  _   _
       z    x    c    v    b    _    n    m    ,    .    _    _
  _    _    _    tab  spc  @lNav @lNum bspc ret  _    _    _
  _    _    _              spc            _    _    _
)
(deflayer Num  
  _     S-1   S-2   S-3   S-4   S-5   _     S-6   S-7   S-8   S-9   S-0   S-`   _
  =     1     2     3     4     5     _     6     7     8     9     0     S-`   _
        S--   -     +     [     ]     _     S-[   S-]   ;     S-;   S-\   _
  _     _     _     _     _     _     _     _     _     _     _     _
  _     _     _              _              _     _     _
)
(deflayer Nav 
  _     home  up    end   pgup  _     _     _     _     _     _     _     _     _
  _     left  0     S-6   S-4   _     _     left  down  up    rght  _     _     _
        _    _      _     _     _     _     _     _     _     _     _     _
  _     _    _      _     _     _     _     _     _     _     _     _
  _     _    _              _              _     _     _
)


(defalias
  aM (tap-hold 200 200 a lmet)
  ;M (tap-hold 200 200 ; rmet)
  sA (tap-hold 200 200 s lalt)
  lA (tap-hold 200 200 l lalt)
  dC (tap-hold 200 200 d lctl)
  kC (tap-hold 200 200 k lctl)
  fS (tap-hold 200 200 f lsft)
  jS (tap-hold 200 200 j lsft)
)

(defalias
  lNum (layer-toggle Num)
  lNav (layer-toggle Nav)
)

